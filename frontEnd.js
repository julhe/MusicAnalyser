var numberOfDifferentNotes;
var input = new Uint16Array(numberOfDifferentNotes);
var input = new Uint16Array(numberOfDifferentNotes);
var fps;

setInterval(createBlocksFromInput, 1000/fps);

function createBlocksFromInput()
{
    Console.Log("Test");
}